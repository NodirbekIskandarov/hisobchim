python-telegram-bot[job-queue]>=21.0,<23
anthropic>=0.40
python-dotenv>=1.0
tzdata; platform_system == "Windows"
fastapi>=0.110
uvicorn[standard]>=0.29
Pillow>=10.0

# Bazani shifrlash (SQLCipher). Faqat Linux serverda kerak — mahalliy
# ishlab chiqishda kalit bo'lmasa oddiy sqlite3 ishlatiladi.
sqlcipher3-binary>=0.5; platform_system == "Linux"
